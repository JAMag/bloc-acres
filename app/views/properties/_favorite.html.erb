<div class="favorite">
  <% if favorite = current_user.favorited(p) %>
    <%= link_to "Remove", [p, favorite], class: 'btn btn-danger', method: :delete %>
  <% else %>
    <%= link_to "Keep", [p, Favorite.new], class: 'btn btn-primary', method: :post %>
  <% end %>
</div>
